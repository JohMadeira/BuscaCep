unit uCommon;

interface

type
  TTipoPesquisa = (eCep, eEndereco);

type
  TLocalBusca = (eBanco, eWebService);

type
  TOperacao = (eConsulta, eAtualiza, eInsere);

type
  TRetornoBusca = (eBD, eWS, eNotFound);

implementation

end.
